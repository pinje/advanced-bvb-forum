stages:
  - build
  - test
  - sonarqube-check
  - deploy

.student-common:
  rules:
    - changes:
      - 'student/*'

student-build:
  extends: .student-common
  stage: build
  script: 
    - echo "Building student"

student-test:
  extends: .student-common
  stage: test
  script:
    - echo "Testing student"

docker-pull:
  stage: sonarqube-check
  tags:
    - docker
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker pull docker:latest

sonarqube-check:
  stage: sonarqube-check
  tags: 
  - docker
  image: maven:3.9.6-eclipse-temurin-17
  script:
    - cd services/backend/student
    - docker compose -f student.gitlab-ci.yml up
    - mvn verify sonar:sonar
  allow_failure: true
  only:
    - merge_requests
    - master
    - main
    - develop
    - dev
