apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway-container
          image: bvbacr.azurecr.io/gateway:1.0.0
          ports:
            - containerPort: 8222
          resources:
            requests:
              memory: "80Mi"
              cpu: "25m"
            limits:
              memory: "896Mi"
              cpu: "250m"
      imagePullSecrets:
        - name: acr-secret-bvb