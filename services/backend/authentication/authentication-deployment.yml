apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authentication
  template:
    metadata:
      labels:
        app: authentication
    spec:
      containers:
        - name: authentication-container
          image: bvbforum.azurecr.io/authentication:3.2.5-c999613
          ports:
            - containerPort: 8081
      imagePullSecrets:
        - name: acr-secret-bvb