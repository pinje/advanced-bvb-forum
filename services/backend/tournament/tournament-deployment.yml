apiVersion: apps/v1
kind: Deployment
metadata:
  name: tournament
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tournament
  template:
    metadata:
      labels:
        app: tournament
    spec:
      containers:
        - name: post-container
          image: bvbacr.azurecr.io/tournament:1.0.0
          ports:
            - containerPort: 8083
          resources:
            requests:
              memory: "448Mi"
              cpu: "125m"
            limits:
              memory: "896Mi"
              cpu: "250m"
      imagePullSecrets:
        - name: acr-secret-bvb